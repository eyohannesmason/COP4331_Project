package controllers;

public interface IController {
    /**
     * It's just a marker for Controllers. Used in BandHeroApp to store the current
     * controller.
     * Ex : Store SignInController when logging in.
     *      Store the ProfileController on successful login.
     */
}
